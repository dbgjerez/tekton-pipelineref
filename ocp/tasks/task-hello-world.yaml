apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: read-msg
  namespace: pipelines
spec:
  steps:
    - name: echo
      image: fedora
      command:
        - cat
      args:
        - msg/README.md
